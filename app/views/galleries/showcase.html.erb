<div class="container">

<div id="blueimp-gallery" class="blueimp-gallery blueimp-gallery-controls">
  <div class="slides"></div>
  <h3 class="title"></h3>
  <a class="prev">‹</a>
  <a class="next">›</a>
  <a class="play-pause"></a>
  <ol class="indicator"></ol>
</div>

	<div class="links" id="links">
	  <% num = 0 %>
	  <% @photolinks.each do |photolink| %>
	  <a href="<%= photolink.content.src + "?imgmax=800" %>" title="Photo <%= num+1 %>" data-gallery="" data-unique-id="tmp_<%= num+1 %>">
	    <%=image_tag(photolink.content.src + "?imgmax=144") %>
	  </a>
	  <% num += 1 %>
	  <% end %>
	</div>


<script>
$(document).ready(function() {
    document.getElementById('links').onclick = function (event) {
        event = event || window.event;
        var target = event.target || event.srcElement,
            link = target.src ? target.parentNode : target,
            options = {index: link, event: event},
            links = this.getElementsByTagName('a');
        blueimp.Gallery(links, options);
    };
});
</script>


</div>

<!-- Picasa image source loop example below -->

<% @photolinks.each do |photolink| %>

<p> <% photolink.content.src + "?imgmax=800" %></p>

<p> <% image_tag(photolink.content.src + "?imgmax=800") %></p>

<% end %>


<!-- Picasa image loop example end -->


